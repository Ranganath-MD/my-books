import React from "react";
import { SafeAreaView } from "react-native-safe-area-context";
import { Text } from "react-native";

export default function Documents() {
	return (
		<SafeAreaView>
			<Text style={{ color: "white"}}>Documents</Text>
		</SafeAreaView>
	);
}
